import { FC } from 'react';
import TodosWrapper from '../components/todos-wrapper/todos-wrapper';

const App: FC = () => {
  return (
    <div className="container">
      <TodosWrapper />
    </div>
  );
};

export default App;
